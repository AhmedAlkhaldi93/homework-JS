const arraySort = [100, 5, 32, 1];
console.log(arraySort.sort( (a, b) => a - b)); // If I usd it like this sort() it will sort is as a String if I need to sort it in another way I have to use condition
